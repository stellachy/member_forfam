// 點擊[＋品種]產生新的一列
const addVarietyBtn = document.getElementById('addVarietyBtn');

addVarietyBtn.addEventListener('click', showVarRow);
function showVarRow () {
  console.log(567);
  // 取得訂購內容的table

  // 找到下一行
}